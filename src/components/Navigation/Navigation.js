const Navigation = () => {
  return (
    <nav
      className="f3 link dim black underline pa3 pointer"
      style={{
        display: "flex",
        justifyContent: "flex-end",
      }}
    >
      <p>Sign out</p>
    </nav>
  );
};

export default Navigation;
